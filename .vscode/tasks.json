{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build firmware",
        "type": "shell",
        "command": "make clean; make -j8 all",
        "group": {
          "kind": "build",
          "isDefault": true,
        },
        "problemMatcher": "$gcc",
        "options": {
            "cwd": "${workspaceRoot}/server"
            // "cwd": "${workspaceRoot}/aesd-char-driver" // for assignment 7.1
        }
      }
    ],
    "isBuildCommand": true
  }